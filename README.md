# analysistools

- This is a tutorial on how to write your python module.

## Install

```bash
pip install git+https://github.com/clap-purdue/analysistools.git
```

- After installation, you can run it on the command line:

```bash
analysistools alice.txt
analysistools alice.txt --top 10
analysistools alice.txt --sentences
```